import TerminalWrapper from '@/components/TerminalWrapper';

export default function Home() {
  return (
    <main className="h-screen w-screen overflow-hidden bg-[#0a0a0a] flex flex-col">
      <TerminalWrapper />
    </main>
  );
}
